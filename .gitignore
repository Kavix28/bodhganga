# ── Secrets — NEVER commit these ─────────────────────────────────────────────
# Environment variable files
.env
.env.local
.env.production
.env*.local
.env*.prod
.env.vercel.prod
backend/.env
frontend/.env
frontend/.env.vercel.prod

# Google Service Account credentials (ALL variants — never commit private keys)
**/google-credentials.json
**/google-credentials.json.json
**/google-qb-credentials.json
**/google-qb-credentials.json.json
backend/src/main/resources/google-credentials.json
backend/src/main/resources/google-qb-credentials.json
# Any file named *-credentials.json or *-service-account.json
*-credentials.json
*-service-account.json
*.keyfile.json

# AWS credential CSVs
AWS/*.csv
AWS/

# ── Backend ───────────────────────────────────────────────────────────────────
backend/target/
backend/.mvn/wrapper/maven-wrapper.jar
*.jar
*.war

# ── Frontend ──────────────────────────────────────────────────────────────────
node_modules/
dist/
.vite/
.dev-logs/

# ── IDE ───────────────────────────────────────────────────────────────────────
.idea/
*.iml
.iws
.ipr
.vscode/
*.swp

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log
logs/

# ── Local Backup Dumps & Audit Artifacts ─────────────────────────────────────
backup_products_*.json
audit_production_products_*.json
*.bak
*.backup
*.before-*
